<div class="row border-bottom">
  <div class="col mt-3"><h2>販売元検索</h2></div>
  <div class="col"></div>
</div>
<div class="row mt-3 ml-3">
  <%= form_with url: creators_search_path, method: :get, local: true do |f| %>
  <%= f.text_field :name %>
  <%= f.submit :search %>
<% end %>
</div>
<div class="row">
  <p class="ptext ml-4">醸造所名をを入力してください<br>
  <%= link_to "無いようであれば追加してください", new_creators_path %></p>
</div>
<div class="row mt-2 ml-3 border-bottom border-dark">
  <h4>検索結果</h4>
</div>

<div class="row mt-2">
   <% @search_creators.each do |creator| %>
  <div class="text-center ml-3 mb-3 border c-box">
    <%= link_to creator.name, creator_path(creator.id), class:"text-dark" %><br>
    <%= link_to creator.url, creator.url, class:"text-dark" %>
  </div>
  <% end %>
</div>