<h2><%= @creator.name %></h2>
<%= @creator.area.name %>
<%= link_to @creator.url, @creator.url %>
<%= @creator.introduction %>
<%= link_to "情報を編集する", edit_creators_path(@creator.id) %>
<h4>ラインナップ</h4>
<% @drinks.each do |drink| %>
<%= link_to drink.name, drink_path(drink.id) %>
<%= drink.alcohol %>%
<%= drink.genre.name %>
<% end %>